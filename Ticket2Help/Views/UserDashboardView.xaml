<Window x:Class="UI.View.UserDashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UI.ViewModel"
        mc:Ignorable="d"
        Title="Dashboard" Height="450" Width="800"
        FontFamily="Segoe UI" FontSize="14">

    <Window.DataContext>
        <vm:UserDashboardViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Bem-vindo ao Painel do Utilizador!"
                   FontSize="22"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Lista de Tickets -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Tickets}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="DarkGray" BorderThickness="1" CornerRadius="5" Margin="5" Padding="10" Background="White">
                            <StackPanel>
                                <TextBlock Text="{Binding Titulo}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding Descricao}" FontStyle="Italic" Foreground="Gray" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding Estado}" Foreground="DarkSlateBlue" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding DataCriacao}" FontSize="12" Foreground="DarkGray" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>